{
  "name": "mapstudio-api-hono",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "studio": "bunx --bun drizzle-kit studio",
    "migration:generate": "bunx --bun drizzle-kit generate",
    "migrate": "bunx --bun drizzle-kit migrate",
    "migration:amend": "bunx --bun drizzle-kit drop && drizzle-kit generate",
    "migration:drop": "bunx --bun drizzle-kit drop"
  },
  "dependencies": {
    "drizzle-orm": "^0.33.0",
    "hono": "^4.6.2",
    "ioredis": "^5.4.1",
    "postgres": "^3.4.4",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.24.2"
  }
}
